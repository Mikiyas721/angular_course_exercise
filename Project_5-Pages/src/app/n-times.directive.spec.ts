import { NTimesDirective } from './n-times.directive';

describe('NTimesDirective', () => {
  it('should create an instance', () => {
    const directive = new NTimesDirective();
    expect(directive).toBeTruthy();
  });
});
